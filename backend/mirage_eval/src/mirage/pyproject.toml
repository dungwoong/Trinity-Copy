[project]
name = "mirage"
version = "0.2.4"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = [
    "setuptools>=49.4.0", 
    "cmake>=3.24",
    "cython>=0.28",
    "z3-solver",
    "graphviz"
]
build-backend = "setuptools.build_meta"
